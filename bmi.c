#include <stdio.h>
#include <cs50.h>
int main(){
    float a=get_int("Enter your name (in m)");
    float b=get_int("Enter your weight(in Kg) ");
    float c= a/(b*b);
    if c<18.5
      printf(a c "underweight")

}
